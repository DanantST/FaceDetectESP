# FaceDetectESP
A portable, header-first C++ library named FaceDetectESP that detects faces in camera frames and returns bounding boxes. The library must be hardware-agnostic (through a CameraAdapter), compatible with Arduino and ESP-IDF builds, fast on ESP32-P4, and packaged as an Arduino/IDF library with examples and tests.
